<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Style x:Key="CED.Button.Base" TargetType="Button">
    <Setter Property="Foreground" Value="{DynamicResource CED.Brush.Accent}"/>
    <Setter Property="Background" Value="{DynamicResource CED.Brush.ButtonDefaultBackground}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource CED.Brush.Accent}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="{DynamicResource CED.Thickness.ButtonPadding}"/>
    <Setter Property="FontWeight" Value="SemiBold"/>
    <Setter Property="MinHeight" Value="{DynamicResource CED.Size.ControlHeight}"/>
    <Setter Property="HorizontalContentAlignment" Value="Center"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Root"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{DynamicResource CED.CornerRadius.Control}"
                  SnapsToDevicePixels="True">
            <Grid>
              <ContentPresenter Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                RecognizesAccessKey="True"/>
              <Border x:Name="StateOverlay"
                      CornerRadius="{DynamicResource CED.CornerRadius.Control}"
                      Background="Transparent"/>
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="StateOverlay" Property="Background" Value="{DynamicResource CED.Brush.ButtonStateOverlayHover}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="StateOverlay" Property="Background" Value="{DynamicResource CED.Brush.ButtonStateOverlayPressed}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="Root" Property="Opacity" Value="0.55"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="CED.Button.Primary" TargetType="Button" BasedOn="{StaticResource CED.Button.Base}">
    <Setter Property="Background" Value="{DynamicResource CED.Brush.PrimaryBlue}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource CED.Brush.PrimaryBlue}"/>
    <Setter Property="Foreground" Value="{DynamicResource CED.Brush.ButtonForegroundOnAccent}"/>
  </Style>

  <Style x:Key="CED.Button.AccentRed" TargetType="Button" BasedOn="{StaticResource CED.Button.Base}">
    <Setter Property="Background" Value="{DynamicResource CED.Brush.SecondaryRed}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource CED.Brush.SecondaryRed}"/>
    <Setter Property="Foreground" Value="{DynamicResource CED.Brush.ButtonForegroundOnAccent}"/>
  </Style>

  <Style x:Key="CED.Button.Apply" TargetType="Button" BasedOn="{StaticResource CED.Button.Base}">
    <Setter Property="Background" Value="{DynamicResource CED.Brush.TertiaryGreen}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource CED.Brush.TertiaryGreen}"/>
    <Setter Property="Foreground" Value="{DynamicResource CED.Brush.ButtonForegroundOnAccent}"/>
  </Style>

  <Style x:Key="CED.Button.Icon" TargetType="Button" BasedOn="{StaticResource CED.Button.Base}">
    <Setter Property="Width" Value="{DynamicResource CED.Size.IconButton}"/>
    <Setter Property="Height" Value="{DynamicResource CED.Size.IconButton}"/>
    <Setter Property="MinWidth" Value="0"/>
    <Setter Property="MinHeight" Value="0"/>
    <Setter Property="Padding" Value="{DynamicResource CED.Thickness.IconButtonPadding}"/>
    <Setter Property="Focusable" Value="False"/>
  </Style>

  <Style x:Key="CED.Button.IconSmall" TargetType="Button" BasedOn="{StaticResource CED.Button.Icon}">
    <Setter Property="Width" Value="{DynamicResource CED.Size.IconButtonSmall}"/>
    <Setter Property="Height" Value="{DynamicResource CED.Size.IconButtonSmall}"/>
  </Style>
</ResourceDictionary>
