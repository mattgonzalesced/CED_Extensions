<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <DataTemplate x:Key="CED.Template.Badge.OverrideIcon">
    <Path Data="{DynamicResource CED.Icon.Account}"
          Fill="{DynamicResource CED.Brush.SelectionBadge}"
          Width="10"
          Height="10"
          Stretch="Uniform"/>
  </DataTemplate>

  <DataTemplate x:Key="CED.Template.Badge.SyncBlockedIcon">
    <Path Data="{DynamicResource CED.Icon.SyncAlert}"
          Fill="{DynamicResource CED.Brush.SecondaryRed}"
          Width="10"
          Height="10"
          Stretch="Uniform"/>
  </DataTemplate>
</ResourceDictionary>
