<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CKT Parameters"
        Height="260"
        Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="TitleBlock"
                   Grid.Row="0"
                   Text="Enter CKT parameters"
                   FontWeight="Bold"
                   Margin="0,0,0,8"/>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="170"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="CKT_Panel_CEDT" Margin="0,0,8,6" VerticalAlignment="Center"/>
            <TextBox   x:Name="PanelBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,6"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="CKT_Circuit Number_CEDT" Margin="0,0,8,6" VerticalAlignment="Center"/>
            <TextBox   x:Name="CircuitBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,6"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="CKT_Rating_CED" Margin="0,0,8,6" VerticalAlignment="Center"/>
            <TextBox   x:Name="RatingBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,6"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="CKT_Load Name_CEDT" Margin="0,0,8,6" VerticalAlignment="Center"/>
            <TextBox   x:Name="LoadNameBox" Grid.Row="3" Grid.Column="1" Margin="0,0,0,6"/>

            <TextBlock Grid.Row="4" Grid.Column="0" Text="CKT_Schedule Notes_CEDT" Margin="0,0,8,6" VerticalAlignment="Center"/>
            <TextBox   x:Name="NotesBox" Grid.Row="4" Grid.Column="1" Margin="0,0,0,6"/>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="OkButton" Content="OK" Width="80" Margin="0,0,8,0" IsDefault="True" Click="OkButton_Click"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="80" IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
