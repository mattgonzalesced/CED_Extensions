<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Parameters"
        Width="540" Height="500"
        MinWidth="420" MinHeight="320"
        WindowStartupLocation="CenterOwner">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" x:Name="HeaderLabel" FontWeight="Bold" Margin="0,0,0,8" TextWrapping="Wrap"/>

    <DockPanel Grid.Row="1" Margin="0,0,0,6">
      <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
        <Button x:Name="AddRowButton" Content="Add" Width="80" Margin="0,0,4,0"/>
        <Button x:Name="DeleteRowButton" Content="Delete" Width="80"/>
      </StackPanel>
    </DockPanel>

    <DataGrid Grid.Row="2" x:Name="ParamGrid"
              AutoGenerateColumns="False"
              CanUserAddRows="False"
              CanUserDeleteRows="False"
              HeadersVisibility="Column"
              GridLinesVisibility="Horizontal"
              SelectionMode="Single"
              ColumnWidth="*"
              RowHeight="22">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Name"
                            Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                            MinWidth="160" Width="1.2*"/>
        <DataGridTextColumn Header="Value"
                            Binding="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                            MinWidth="200" Width="1.8*"/>
      </DataGrid.Columns>
    </DataGrid>

    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
      <Button x:Name="OkButton" Content="OK" Width="100" Margin="0,0,8,0" IsDefault="True"/>
      <Button x:Name="CancelButton" Content="Cancel" Width="100" IsCancel="True"/>
    </StackPanel>
  </Grid>
</Window>
