<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Buckets"
        Width="460" Height="540"
        MinWidth="380" MinHeight="380"
        WindowStartupLocation="CenterOwner">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" x:Name="HeaderLabel" FontWeight="Bold" Margin="0,0,0,8" TextWrapping="Wrap"/>
    <TextBlock Grid.Row="1" x:Name="SubLabel" Foreground="Gray" Margin="0,0,0,8" TextWrapping="Wrap"
               Text="Tick the buckets that apply. The first bucket in this list (top to bottom) is the primary; stacked buckets accumulate equipment."/>

    <ListBox Grid.Row="2" x:Name="BucketList"
             SelectionMode="Single">
      <ListBox.ItemTemplate>
        <DataTemplate>
          <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Content="{Binding Label}"
                    ToolTip="{Binding ToolTip}"
                    Margin="2"/>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>

    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
      <Button x:Name="ResetButton" Content="Reset to auto" Width="120" Margin="0,0,8,0"/>
      <Button x:Name="OkButton" Content="OK" Width="100" Margin="0,0,8,0" IsDefault="True"/>
      <Button x:Name="CancelButton" Content="Cancel" Width="100" IsCancel="True"/>
    </StackPanel>
  </Grid>
</Window>
