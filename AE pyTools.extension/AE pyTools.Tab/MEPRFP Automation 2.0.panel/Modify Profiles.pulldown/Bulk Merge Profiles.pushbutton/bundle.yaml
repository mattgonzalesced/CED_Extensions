title: Bulk Merge Profiles
tooltip: |
  Bulk-add aliases from a CSV.
  CSV format: header row with 'source' (or source_id / source_name) and
  'target' (or target_id / target_name / alias). Each row appends the
  target value as an alias to the source's merged_aliases list.
is_beta: false
