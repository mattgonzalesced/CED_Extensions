title: Follow Parent
tooltip: |
  Re-position placed children to follow their parent's current location
  and rotation. Uses each child's Element_Linker payload to find its
  parent and the LED's stored offset to compute the new pose. Skips
  children that are already aligned within tolerance.
is_beta: false
