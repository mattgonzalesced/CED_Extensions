<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Family Database"
        Height="720"
        Width="1240"
        MinHeight="560"
        MinWidth="980"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="240" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="220" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="220" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="180" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="Category:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <ComboBox x:Name="CategoryCombo"
                      Grid.Column="1"
                      Margin="0,0,16,0"
                      MinWidth="220"
                      IsTextSearchEnabled="True" />

            <TextBlock Grid.Column="2"
                       Text="Family Filter:"
                       VerticalAlignment="Center"
                       Margin="0,0,8,0" />
            <TextBox x:Name="FamilyFilterBox"
                     Grid.Column="3"
                     Margin="0,0,16,0" />

            <TextBlock Grid.Column="4"
                       Text="Client Filter:"
                       VerticalAlignment="Center"
                       Margin="0,0,8,0" />
            <TextBox x:Name="ClientFilterBox"
                     Grid.Column="5"
                     Margin="0,0,16,0" />

            <TextBlock Grid.Column="6"
                       Text="Project # Filter:"
                       VerticalAlignment="Center"
                       Margin="0,0,8,0" />
            <TextBox x:Name="ProjectFilterBox"
                     Grid.Column="7" />
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="380" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Parameter Filter:"
                       VerticalAlignment="Center"
                       Margin="0,0,8,0" />
            <TextBox x:Name="ParameterFilterBox"
                     Grid.Column="1"
                     IsEnabled="False"
                     ToolTip="Parameter filtering placeholder for a future update."
                     Text="Coming soon" />
            <TextBlock Grid.Column="2"
                       Text="(placeholder for future parameter filtering)"
                       Foreground="#777"
                       VerticalAlignment="Center"
                       Margin="8,0,0,0" />
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="70" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="340" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="#D0D0D0" BorderThickness="1" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="Family Files"
                               FontSize="14"
                               FontWeight="Bold"
                               Margin="0,0,0,6" />
                    <ListBox x:Name="FamilyFilesList"
                             Grid.Row="1"
                             HorizontalContentAlignment="Stretch">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding [display_name]}"
                                           TextTrimming="CharacterEllipsis" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <Border Grid.Column="2" BorderBrush="#D0D0D0" BorderThickness="1" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="Family Types"
                               FontSize="14"
                               FontWeight="Bold" />
                    <TextBlock x:Name="SelectedFileText"
                               Grid.Row="1"
                               Margin="0,2,0,6"
                               Foreground="#555"
                               TextTrimming="CharacterEllipsis" />

                    <ListView x:Name="TypeRowsList"
                              Grid.Row="2"
                              HorizontalContentAlignment="Stretch">
                        <ListView.View>
                            <GridView AllowsColumnReorder="False">
                                <GridViewColumn Header="Type Name" Width="280">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding [type_name]}" TextTrimming="CharacterEllipsis" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Product Type" Width="220">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding [product_type]}" TextTrimming="CharacterEllipsis" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Client" Width="200">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding [client_name]}" TextTrimming="CharacterEllipsis" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Project #" Width="150">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding [project_number]}" TextTrimming="CharacterEllipsis" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </Border>

            <StackPanel Grid.Column="3"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center">
                <Button x:Name="StageAddButton"
                        Content="&gt;"
                        Width="44"
                        Height="32"
                        Margin="0,0,0,8"
                        ToolTip="Add selected family file to staging." />
                <Button x:Name="StageRemoveButton"
                        Content="&lt;"
                        Width="44"
                        Height="32"
                        ToolTip="Remove selected family from staging." />
            </StackPanel>

            <Border Grid.Column="5" BorderBrush="#D0D0D0" BorderThickness="1" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="Import Staging"
                               FontSize="14"
                               FontWeight="Bold" />
                    <TextBlock x:Name="StagingSummaryText"
                               Grid.Row="1"
                               Margin="0,2,0,6"
                               Foreground="#555"
                               Text="0 families staged" />

                    <ListView x:Name="StagingList"
                              Grid.Row="2"
                              HorizontalContentAlignment="Stretch">
                        <ListView.View>
                            <GridView AllowsColumnReorder="False">
                                <GridViewColumn Header="Family" Width="165">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding [family_name]}" TextTrimming="CharacterEllipsis" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Client" Width="95">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding [client_name]}" TextTrimming="CharacterEllipsis" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Project #" Width="78">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding [project_number]}" TextTrimming="CharacterEllipsis" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="StatusText"
                       VerticalAlignment="Center"
                       Text="Ready" />

            <Button x:Name="ImportButton"
                    Grid.Column="1"
                    Content="Import Family"
                    Margin="8,0,0,0"
                    Padding="14,6" />
            <Button x:Name="ExportButton"
                    Grid.Column="2"
                    Content="Export Family"
                    Margin="8,0,0,0"
                    Padding="14,6" />
            <Button x:Name="RefreshButton"
                    Grid.Column="3"
                    Content="Refresh"
                    Margin="8,0,0,0"
                    Padding="14,6" />
            <Button x:Name="CloseButton"
                    Grid.Column="4"
                    Content="Close"
                    Margin="8,0,0,0"
                    Padding="14,6" />
        </Grid>
    </Grid>
</Window>
