title: Export YAML File
tooltip: |
  Export the active Extensible Storage dataset to a YAML file on disk.
  This writes the current in-model state, including recent profile and placement metadata edits.
  Use it to create shareable snapshots for review, handoff, or version control.
  The exported file can be re-imported later as a clean starting dataset.
is_beta: false
