title: Place CAD Elements by Category & Profile
tooltip: |
  Place CSV-driven elements through active YAML definitions with profile-level filtering.
  This version supports both category selection and explicit profile selection before placement.
  Coordinate rows are processed only for definitions that pass the chosen filters.
  Use this when you need fine-grained control over which profiles are instantiated.
