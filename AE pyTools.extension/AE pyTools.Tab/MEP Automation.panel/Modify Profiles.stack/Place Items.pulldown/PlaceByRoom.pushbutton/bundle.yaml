title: Place by Room
tooltip: |
  Place selected profile types at room-center points using room-name keyword matching.
  The command processes linked room data and maps each match to configured profile definitions.
  Placements use active YAML content so profile behavior remains consistent with library settings.
  Use this for repeatable room-based layouts where naming patterns drive element selection.
is_beta: false
