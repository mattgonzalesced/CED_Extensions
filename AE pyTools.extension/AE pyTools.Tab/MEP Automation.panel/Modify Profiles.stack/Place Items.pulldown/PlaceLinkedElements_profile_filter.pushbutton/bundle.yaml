title: Place Linked Elements (Profile Filter)
tooltip: |
  Place only selected profile groups from the active YAML dataset.
  Hosts are still matched by name, but only chosen profile groups are eligible.
  This is useful for phased rollout, targeted reruns, or discipline-specific placement passes.
  Created instances receive full Element_Linker metadata for reporting and cleanup.
is_beta: false
