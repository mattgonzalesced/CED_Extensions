title: Unmerge Profiles
tooltip: |
  Detach a merged profile from its truth-source group metadata.
  The selected profile becomes an independent definition while keeping its own content.
  This is useful when one variant needs to diverge from a shared merged configuration.
  Only YAML metadata is changed; existing model instances are not deleted.
is_beta: false
