title: Delete YAML Profiles
tooltip: |
  Delete selected linked element definitions from an equipment profile in active YAML.
  If a definition becomes empty, related entries can be removed to prevent orphaned structures.
  Child relationships and dependent references are cleaned up during the save process.
  Use this when retiring outdated profile content while preserving dataset integrity.
is_beta: true
