title: Add YAML Profiles
tooltip: |
  Add selected Revit element types to an existing or new equipment definition.
  The command records labels, category data, offsets, parameters, tags, and linker payloads.
  It supports extending merged profile groups by propagating new type entries where applicable.
  Use this when introducing new modeled assemblies into the active YAML library.
is_beta: true
