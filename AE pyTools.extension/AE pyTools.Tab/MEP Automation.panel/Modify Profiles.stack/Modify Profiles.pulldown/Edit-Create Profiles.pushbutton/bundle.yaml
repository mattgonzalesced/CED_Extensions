title: Edit/Create Profiles
tooltip: |
  Select a linked parent element and load its profile context for editing.
  Capture selected child elements to store updated offsets, tags, and wire-related metadata.
  The result updates or creates profile content directly in active Extensible Storage.
  Use this for iterative refinement when modeled assemblies evolve during design.
is_beta: false
