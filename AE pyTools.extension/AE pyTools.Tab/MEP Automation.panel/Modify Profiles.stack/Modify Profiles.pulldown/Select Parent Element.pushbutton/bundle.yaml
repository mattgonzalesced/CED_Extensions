title: Select Parent Element
tooltip: |
  Attach selected child elements to a chosen parent and save them as linked definitions.
  The command resolves or creates the parent equipment definition in active YAML.
  Offsets and rotations are computed from the parent origin so placement remains reproducible.
  Element_Linker metadata is written to selected children for downstream tracking and QA.
is_beta: true
