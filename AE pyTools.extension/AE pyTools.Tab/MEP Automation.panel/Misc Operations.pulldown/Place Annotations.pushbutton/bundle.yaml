title: Place Annotations
tooltip: |
  Place tags, keynotes, and text notes from active YAML definitions onto matching instances.
  The command reads stored annotation offsets and applies them in the current view context.
  Only compatible hosts are annotated, with filtering based on recorded metadata.
  Use this after placement updates when annotation content needs to be regenerated consistently.
is_beta: false
