title: Update Profiles
tooltip: |
  Sync placed Element_Linker data back into the active YAML profile definitions.
  The update captures current offsets, parameters, tags, and text-note metadata from the model.
  This keeps stored profiles aligned with field changes made after placement.
  Save and export when needed so downstream projects can consume the refreshed definitions.
is_beta: false
