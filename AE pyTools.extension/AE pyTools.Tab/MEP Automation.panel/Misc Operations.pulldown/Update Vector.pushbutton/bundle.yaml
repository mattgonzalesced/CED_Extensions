title: Update Vector
tooltip: |
  Recalculate geometric vectors for an existing equipment definition from selected elements.
  The command updates centroid-relative offsets to match the current arrangement in model space.
  Profile metadata stays focused on vector geometry instead of rebuilding the entire definition.
  Use this when layout shifts but the same profile identity should remain intact.
is_beta: false
