title: Schema Viewer
tooltip: |
  Run a schema sanitation diagnostic focused on YAML hash-key handling behavior.
  The command reads a prototype YAML file and applies internal hash-key normalization logic.
  Output shows whether hash characters remain and whether sanitized content differs from raw text.
  Use this for parser troubleshooting; it is a read-only diagnostic and does not edit model data.
is_beta: false
