<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Parent Parameter Conflicts"
        Height="600"
        Width="1000"
        WindowStartupLocation="CenterScreen">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Name="HeaderText"
               Text="Resolve parent parameter discrepancies detected after sync."
               Margin="0,0,0,6"/>

    <StackPanel Name="ParamDefaultsPanel"
                Grid.Row="1"
                Margin="0,0,0,8"/>

    <ScrollViewer Grid.Row="2"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto">
      <Grid Name="ConflictGrid"/>
    </ScrollViewer>

    <StackPanel Grid.Row="3"
                Orientation="Horizontal"
                HorizontalAlignment="Right"
                Margin="0,8,0,0">
      <Button Name="ApplyButton" Content="Apply" Width="90" Margin="0,0,8,0"/>
      <Button Name="CancelButton" Content="Cancel" Width="90"/>
    </StackPanel>
  </Grid>
</Window>
